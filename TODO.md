# Open items

- [ ] One repetition (seed 17) of the portfolio T=5001 full-sample DRO run
      is still computing. When it finishes: sync the results in, rerun
      `python -m portfolio.plots ... --T 5001 --init`, and commit the
      regenerated figures. Only the DRO/SAA confidence bands in the
      portfolio T5000 figures change (29 -> 30 repetitions); no reported
      numbers move.
